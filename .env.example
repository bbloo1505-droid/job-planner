# Stage 1 does not require secrets. Geocoding and travel estimates are local.
# Do not put API keys in frontend code.
# Stage 2 (not implemented): Azure / Entra / approved mapping keys must be server-side only.
